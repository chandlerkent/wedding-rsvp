
import { canChooseMeal } from 'wedding/helpers/can-choose-meal';
import { module, test } from 'qunit';

module('Unit | Helper | can choose meal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canChooseMeal([42]);
  assert.ok(result);
});

